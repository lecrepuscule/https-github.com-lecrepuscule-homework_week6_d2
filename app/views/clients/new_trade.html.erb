<h1>New Trade</h1>

<%= form_for @trade, url: {controller: 'clients', action: 'create_trade'} do |f| %>

  <%= f.collection_select(:portfolio_id, Portfolio.all, :id, :name) %>

  <%= f.label :quantity %>
  <%= f.number_field :quantity %>

  <%= f.submit %>

<% end %>